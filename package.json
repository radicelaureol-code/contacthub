{
  "name": "contacthub",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "dev": "cd server && npm run dev",
    "build": "node scripts/build.js",
    "seed": "cd server && node server/src/scripts/seed.js",
    "start": "cd server && npm start"
  },
  "dependencies": {
    "fs-extra": "^11.2.0"
  }
}